import React from 'react';
import { Typography } from 'antd';

const HeaderPage = () => (
  <>
    <Typography.Title level={2}>房贷提前还款计算器(暂时只支持等额本息)</Typography.Title>
  </>
);

export default HeaderPage;
